#################################################################################
# ETF Price Forecasting Project
#
# Premature pipeline:
# 1. Webcrawling (Yahoo Finance API) for ETF price history (Use multiple representative China ETFs for training, e.g., 510300.SS and 510050.SS; use SPY, QQQ, IEUR for cross-market feature engineering with strict no-lookahead time alignment).
# 2. Data Cleaning; visualization; feature engineeing; ready to pass in to the model in .csv format.
# 3. Train, validation, test set spliting ratio?(Set as hyperparameter, for Bayesian tuning)
# 4. Model selection: currently we have XGBoost, MLP as baseline model option (more to be explored browse the web to find more experimental algorithms).
# 5. Predict Open-to-prev-close gap (log return). Back test with past years' trading data. 
# 
#
# Advanced thoughts:
# 1. Use the Premature pipeline as the backend logic, build a website (or alternatively use Grafana to build an online dashbard), 
#    which enable users to interact with the forcast flow. Give out buy/sell strategies, for crediability we can demo with past 7 trading days Sharpe Ratio diagram. 
#    Design details (UI, functionality, ect.)
# 2. More advanced thought: Automate model updating progress, for each financial season, retrain the model to fit in the current market trend.
#################################################################################

Phase 1: Data retreival

Yahoo finance API 
--> Store the data in dataframe, for current phase focus on ETF in China market and train on multiple representative China ETFs. Do consider EU/US ETFs for cross-market Feature Engineering with strict timezone/calendar alignment (no lookahead).
    For universal application, multiple Chinese ETFs are selected for comprehensive representation(510050,510300,510500,159915,588000). Cross market ETFs covering US&EU (SPY,QQQ,IEUR).
--> reformulate the data structure after downloading. Yfinance can sometimes give out ill structured data. Question to be disgussed: Data fetch time window range automation given futrue model auto update design.
--> Data cleaning and feature engineering (We can try to construct a dummy volitality mitrix, don't have to be strictly correct).
    Cleaning: Due to hoilday difference between the US and China market, use statistical imputation on US&EU related columns to avoid dropping core Chinese market info. DO NOT IMPUTE NAN CELLS in CORE CHINESE ETFs, DROP them directly.
    Possible features:
    refer to .\ETF_feature_engineering_summary.txt 
--> Introduce GRA for feature selection (Grey relational analysis)
--> Integrate into .csv format

Phase 2: Data loader
--> Set splition ratio as hyperparameter, later logged into arguements.
--> Train loader, validation loader and test loader implementation.
--> Load and ready to train.

Phase 3: Train model
--> XGBoost &/ MLP For 1 step ahead, try XGBoost first.
--> Metrixes (MSE, R^2, RMSE, MAE)
--> Hyperparameter tuning, Bayesian with pruning.
--> Compare performance
--> Use the best model for dashboard prediction.

Phase 4: To be continued

Open Universe Design (for website deployment)
--> Keep feature schema ID-agnostic (do not rely on ETF one-hot identity as core feature).
--> For each prediction request, require fixed input columns and minimum lookback history (e.g., 60 trading days).
--> If history is insufficient, return "not_enough_history" instead of forced prediction.
--> Keep EU/US cross-market features lagged and aligned by China decision timestamp (strict no-lookahead).
--> Validate generalization by leave-one-ETF-out tests to simulate unseen ETF inference.
